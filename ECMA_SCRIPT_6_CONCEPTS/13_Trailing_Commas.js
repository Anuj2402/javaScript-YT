/*
Trailing commas are commas that are placed at the end of a list of items in a JavaScript object, array, or function parameter list. They were introduced in ES6 and are now supported by all major browsers.
*/

function add(param1){
    const example ={
        name : "anuj",
    }; //Trailing commas
    console.log(example);
};

add(2)